mixin loginFormWes()
	form(action="/loginwes" method="POST")
		label(for="email") Email Address
		input(type="email" name="email")
		label(for="password") Password
		input(type="password" name="password")
		input.button(type="submit" value="Log in")